#include <bits/stdc++.h>
using namespace std;

int main()
{
    string str = "sandeep";
    cout << str[0] << "\n";

    // to get the length of the string
    int n = str.size();
    cout << n << "\n";

    // access last character
    cout << str[n - 1];
}